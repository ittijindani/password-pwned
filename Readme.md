```USAGE:
	hibp_password.py [OPTIONS] [password1, password2...]

OPTIONS
-h, -H, -help, -Help
	Print a usage message briefly summarizing these command-line options

-i, --ifile=
	Input file name containing one password on each line

-o, --ofile=
	Outout file to store the PWNED resuts. The default is STDOUT
```
